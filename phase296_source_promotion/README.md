# Phase 296 — Source Promotion and Rollback

A source candidate becomes active only after the sealed train/held-out/regression gate passes. Rejected candidates do not mutate the active source. Promotion history is fingerprinted and rollback is explicit.
