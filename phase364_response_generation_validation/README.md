# Phase 364 — Response Generation Validation

Adds a fail-closed validation boundary for generated output. It validates configured limits and mode policy without rewriting, inventing, or semantically modifying model output.
