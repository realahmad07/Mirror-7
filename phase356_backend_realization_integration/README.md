# Phase 356 — Backend Realization Integration

Integrates the Phase 353–355 response-realization contract into the backend
session boundary.

The backend exposes only data already verified by the engine's semantic state.
It does not invent response text, actions, plans, or model outputs.
