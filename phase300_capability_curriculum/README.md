# Phase 300 — Capability Curriculum

Each target capability receives deterministic train, held-out, and regression material so the frontier loop can keep testing improvement rather than relying on one static benchmark.
