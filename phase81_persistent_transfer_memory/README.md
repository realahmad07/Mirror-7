# Phase 81 — Persistent Transfer Memory

Stores bounded environment-tagged transition evidence with provenance, confidence, shape checks, distance rejection, and JSON persistence.

Boundary: explicit numeric signatures; transfer is rejected when evidence is absent or below threshold.

Run: python -m pytest -q phase81_persistent_transfer_memory
