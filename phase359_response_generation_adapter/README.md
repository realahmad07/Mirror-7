# Phase 359 — Response Generation Adapter

Defines the model boundary used by response generation. A model only needs `generate(request)`. The adapter preserves the verified Phase 355 contract and delegates generation without inventing actions or context.
