# Phase 355 — Backend Realization Contract

Defines the UI-independent contract handed to a future response-realization implementation.

It transports only existing semantic context, verified plan actions, output mode, and observation.
