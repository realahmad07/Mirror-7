# Phase 362 — Response Generation Configuration

Adds an immutable, model-agnostic configuration boundary for response realization limits and allowed output modes. It does not choose a model or alter Mirror 7 reasoning.
