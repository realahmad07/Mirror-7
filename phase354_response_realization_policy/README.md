# Phase 354 — Response Realization Policy

Maps verified semantic output intent to a bounded downstream realization policy.

The policy is metadata only: it cannot invent actions or modify world state.
