# Mirror 7 — Training Session Handoff — 2026-09-20

## Exact continuation point

This file is the handoff for continuing Mirror 7 training work without losing the current state.

### Compute

- Google Colab Pro is active.
- Current runtime: NVIDIA A100-SXM4-40GB.
- CUDA is available.
- Colab is the only planned training platform for the current stage.
- Google Drive is mounted at `/content/drive`.
- Persistent Mirror storage: `/content/drive/MyDrive/Mirror7/`.

### Code/data separation

- GitHub repository: `realahmad07/Mirror-7`.
- GitHub is the source of truth for training code and architecture.
- Google Drive stores training datasets and checkpoints.
- Colab clones GitHub into `/content/Mirror-7` and mounts Drive separately.

### Dataset

Source dataset:
- OpenAssistant OASST2.

Initial filtered dataset:
- File: `mirror_oasst2_filtered_v2.jsonl`
- Total: 14,801
- Train: 11,838
- Validation: 1,471
- Test: 1,492
- Dataset fingerprint: `8fb46e7cffa41655d8c05c1b1b0a9e44070f94da2c788ab6a79c1cead1f89437`

Quality audit:
- File: `quality_audit_v1.json`
- Total: 14,801
- Mean quality field: 1.0000 (therefore the quality field is not useful as a discriminator)
- Unique target texts: 14,733
- Duplicate-target excess: 68
- Very short targets (<40 bytes): 334
- Very long targets (>8000 bytes): 8
- User equals target: 4
- High word repetition: 39
- Repeated 4-gram flag: 1,418
- Boilerplate flag: 423

Important curation finding:
- Repeated 4-gram and boilerplate flags are not sufficient by themselves; some legitimate long/code answers trigger them.
- Conservative automatic curation was chosen.

Curated dataset:
- File: `mirror_oasst2_curated_v1.jsonl`
- Original: 14,801
- Removed: 174
- Retained: 14,627
- Removal reasons:
  - self-referential boilerplate: 99
  - duplicate target: 68
  - user equals target: 4
  - generic social response: 3
- Original filtered dataset remains untouched.

### Training results so far

Initial model baseline:
- Byte-level GRU response model.
- Approximately 1.7M parameters.
- 263 vocabulary/control entries.
- 192 embedding.
- 384 hidden.
- 2 layers.
- 0.10 dropout.
- 4,096 max sequence bytes.

Real OASST2 training:
- 5 epochs on A100.
- Training loss: 2.2112 -> 1.3680.
- Validation loss: 1.8740 -> 1.3640.
- Checkpoint: `mirror7_response_v1.pt`.

The checkpoint was NOT accepted for deployment:
- Held-out generation was nonempty but incoherent/repetitive.
- Earlier evaluation reported 64/64 nonempty and 0/64 exact matches.
- The output repeatedly collapsed into phrases such as “state of the state...”.
- The attached evaluation evidence shows the same behavior across multiple held-out examples.

Generation fixes already pushed:
- Full prompt now primes recurrent state.
- First generated byte no longer incorrectly feeds the RESPONSE control token twice.
- Latest related commits include `43a96c9` and the later training/evaluation fixes.

Training pipeline fixes already pushed:
- EOS is included in the response training loss.
- Training examples are shuffled per epoch with deterministic seeding.
- Evaluation exact-match reporting now accounts for target length limits and reports truncated-target counts.
- Dataset validator can now run both as:
  - `python -m training.validate_dataset`
  - `python training/validate_dataset.py`
- Latest validator compatibility commit: `80646ec91890b6e590956485ec54c3d05795f94b`.

### Tiny overfit diagnostic

A tiny five-example training diagnostic was run to distinguish a broken pipeline from a weak model.

Dataset:
- 5 train examples
- 1 validation example

100-epoch tiny training:
- Train loss fell from 6.6397 to 0.00107.
- Validation loss intentionally rose because the validation example was unseen.

Training-set evaluation:
- 5/5 exact matches.
- Exact-match rate: 1.0.
- This proves the training/checkpoint/generation pipeline can learn and reproduce a tiny dataset.

Therefore:
- The A100 is functioning.
- The basic training implementation can learn.
- The major weakness is the small 1.7M language model and the difficulty of the real language dataset, not a dead training pipeline.

### Current target

Do NOT retrain the 1.7M model.

Next target:
- Build and train approximately 10M parameters.
- Exact verified configuration:
  - embedding_dim = 256
  - hidden_dim = 1024
  - num_layers = 2
  - dropout = 0.10
  - max_sequence_bytes = 4096
- Verified parameter count: 10,574,855 (~10.57M).

### Quality rule

Mirror training follows a strict “quality over quantity” principle.

Do not increase dataset size merely for scale.
Prefer clean, useful, diverse examples.
Do not delete flagged examples solely because a simple heuristic fired.
Keep the original dataset and all curated versions for reproducibility.

### Immediate next actions

1. Validate `mirror_oasst2_curated_v1.jsonl`.
2. If validation passes, train the 10.57M model on A100.
3. Save checkpoint to Drive.
4. Evaluate on held-out test data.
5. Compare against the 1.7M baseline.
6. Do not integrate into production until qualitative and quantitative evaluation passes.
7. If 10M remains weak, improve architecture/training/data before scaling further.

### Important operational rule

Do not waste A100 time on diagnostics that can run on CPU.
Do not delete the original filtered dataset.
Do not replace the validated dataset without creating a new version and fingerprint.
Do not call a model “ready” from loss alone; generation quality and held-out evaluation are required.

## Colab paths

Repository:
`/content/Mirror-7`

Drive:
`/content/drive/MyDrive/Mirror7/`

Curated dataset:
`/content/drive/MyDrive/Mirror7/data/mirror_oasst2_curated_v1.jsonl`

Current 1.7M checkpoint:
`/content/drive/MyDrive/Mirror7/mirror7_response_v1.pt`

Tiny diagnostic checkpoint:
`/content/drive/MyDrive/Mirror7/pipeline_test.pt`

