# Phase 284 — Bounded Algorithm Mutation

Phase 284 introduces controlled algorithm mutation: single-operation replacement and bounded append. Every mutation remains inside the declarative Phase 283 allow-list and has a finite child budget. No arbitrary Python/source rewriting is performed.
