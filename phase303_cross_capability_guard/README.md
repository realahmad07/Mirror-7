# Phase 303 — Cross-Capability Regression Guard

A capability upgrade is rejected when it materially damages protected capabilities. This creates a frontier-level regression barrier rather than optimizing one benchmark in isolation.
