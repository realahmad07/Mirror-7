# Phase 294 — Sealed Source Evaluation

Validated candidate source is evaluated in a separate isolated Python process with hidden targets retained by the parent evaluator. Process failures and invalid candidates fail closed.
