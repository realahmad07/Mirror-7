# Phase 76 — Autonomous Curriculum

Adds a bounded experiment-selection layer. Candidates are scored, invalid scores are rejected, experiments consume a hard budget, and a stop condition can terminate the curriculum when a target is reached.

This is an implementation scaffold, not AGI or a certified autonomous scientist. Full multi-seed/adversarial certification remains separate.
